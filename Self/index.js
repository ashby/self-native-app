/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SelfApp from './app/index.js';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SelfApp);
